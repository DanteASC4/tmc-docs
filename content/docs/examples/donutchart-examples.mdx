---
title: Donutchart Examples
description: A cup of coffee and a donut, anyone?
---

# `donutchart`

```ts
const myChart = donutchart({
  data: [50, 100, 30],
});

document.body.appendChild(myChart);
```

Just a reminder that `piechart` returns an SVG, so you'll want to add it to the DOM.

## Quick Start

The most basic possible usage:

```ts
donutchart({
  data: [30, 30, 30],
  fillColors: ['#00ffff', '#ff00ff', '#ffff00'],
});
```

<p className="text-xs text-italic">
  {' '}
  colors are given to make the slices visible, but are not required, though
  without color or stroke colors you'll likely end up with just a circle
  visually!{' '}
</p>

[The First Chart](/imgs/examples/donutchart/ex0.svg)

Of course it works with different data points too:

```ts
donutchart({
  data: [300, 300, 300, 300],
  fillColors: ['#00ffff', '#ff00ff', '#ffff00', '#d4453a'],
});
```

[Basic donut chart with more datapoints](/imgs/examples/donutchart/ex0a.svg)

```ts
donutchart({
  data: [50, 50],
  fillColors: ['#00ffff', '#ff00ff'],
});
```

[Basic donut chart with less datapoints](/imgs/examples/donutchart/ex0b.svg)

```ts
donutchart({
  data: [400, 100, 300, 70, 130],
  fillColors: ['#00ffff', '#ff00ff', '#ffff00', '#d4453a', '#3e9503'],
});
```

[Basic donut chart with more and larger datapoints](/imgs/examples/donutchart/ex0c.svg)

## Label Options

You can use any string for the `labels` option, and they will be placed in the center of each donut slice.

Coloring is done via `labelColors`, which takes an array of colors that correspond to the labels. If not provided, the labels will default to white.

```ts
donutchart({
  data: [30, 30, 30],
  fillColors: ['#00ffff', '#ff00ff', '#ffff00'],
  labels: ['Cyan', 'Magenta', 'Yellow'], // [!code ++]
  labelColors: ['#000000'], // [!code ++]
});
```

[A donut chart with labels and label colors](/imgs/examples/donutchart/ex1.svg)

### Data Labels

Or you can use the `dataLabels` option to show the data values instead of custom labels:

<p className="text-xs text-italic"> `labelColors` also is used to color `dataLabels`. </p>
```ts
donutchart({
	data: [30, 30, 30],
	fillColors: ['#00ffff', '#ff00ff', '#ffff00'],
	dataLabels: 'literal', // [!code ++]
	labelColors: ['#000000'],
});
```

[A donut chart with data labels](/imgs/examples/donutchart/ex2.svg)

The `dataLabels` option also accept `"percentage"`:

```ts
donutchart({
  data: [30, 30, 30],
  fillColors: ['#00ffff', '#ff00ff', '#ffff00'],
  // [!code word:percentage]
  dataLabels: 'percentage',
  labelColors: ['#000000'],
});
```

[A donut chart with percentage data labels](/imgs/examples/donutchart/ex3.svg)

### Center Label

The `centerLabel` option allows you to place a label in the center of the donut chart, which can be used to show a title or other value.

```ts
donutchart({
  data: [30, 30, 30],
  fillColors: ['#00ffff', '#ff00ff', '#ffff00'],
  centerLabel: 'A center label', // [!code ++]
});
```

[A donut chart with a center label](/imgs/examples/donutchart/ex3a.svg)

If you set the `centerLabel` option to `"sum"` the center label will show the sum of the data values:

```ts
donutchart({
  data: [30, 30, 30],
  fillColors: ['#00ffff', '#ff00ff', '#ffff00'],
  // [!code word:sum]
  centerLabel: 'sum',
});
```

[A donut chart with the sum of the data values as the center label](/imgs/examples/donutchart/ex3b.svg)

**Adjusting center label styles**

There are a couple options that allow for adjustment of common title text styles for the center label.

- `centerLabelFontSize`
  - defaults to `32`
- `centerLabelFontWeight`
  - defaults to `bold`
- `centerLabelFontFamily`
  - defaults to `monospace`

The `centerLabel` will also get a class applied by default that you can use to override the default styling - `"tmc-pie-center-label"`.

You can also use the `centerLabelClass` option to add your own class to the center label `<text>` element.

```ts
donutchart({
  data: [30, 30, 30],
  fillColors: ['#00ffff', '#ff00ff', '#ffff00'],
  centerLabel: 'A center label', // [!code ++]
  classes: {
    centerLabelClass: 'my-center-label', // [!code ++]
  },
});
```

## Styling

While styling many options is easily achievable with CSS, there are various styling options available for quick & easy use.

### Size & Spacing

The `size` option will change the size of the chart. By default the `size` is `300` which creates a `300px` square, note that all donut charts are squares!

```ts
donutchart({
  data: [30, 30, 30],
  fillColors: ['#00ffff', '#ff00ff', '#ffff00'],
  size: 50, // [!code ++]
});
```

[A very small donut chart](/imgs/examples/donutchart/ex4.svg)

You can also change the `padding` which is the interior spacing from the SVG edges to the donut chart itself.

Note that currently `padding` is used uniformly on all sides.

```ts
donutchart({
  data: [30, 30, 30],
  fillColors: ['#00ffff', '#ff00ff', '#ffff00'],
  padding: 50, // [!code ++]
});
```

[A donut chart with a lot of padding](/imgs/examples/donutchart/ex5.svg)

### Coloring

While `fillColors` is the way to color each slice, you can also set `strokeColors`, and `strokeWidths` to customize the stroke of each slice.

If you want them all to be uniform, simply provide a single value for each option.

```ts
donutchart({
  data: [30, 30, 30],
  fillColors: ['#00ffff', '#ff00ff', '#ffff00'],
  strokeColors: ['#ffffff'],
  strokeWidths: [5],
});
```

[A donut chart with white strokes](/imgs/examples/donutchart/ex6.svg)

Additionally, just like other chart types, gradient fills are supported!

```ts
donutchart({
  data: [30, 30, 30],
  gradientColors: ['#ff00ff', '#00ffff'],
  gradientDirection: 'left-to-right', // [!code ++]
  gradientMode: 'continuous', // [!code ++]
  strokeColors: ['#ffffff'],
});
```

[A donut chart with a continuous gradient fill](/imgs/examples/donutchart/ex7.svg)
